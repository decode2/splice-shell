[package]
name = "splice-compat"
version = "0.1.0"
description = "Evidence-based AI CLI compatibility matrix"
license.workspace = true
repository.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
