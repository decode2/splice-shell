[package]
name = "splice-core"
version = "0.1.0"
description = "Core domain types for Splice Shell"
license.workspace = true
repository.workspace = true
edition.workspace = true

[dependencies]
fs2 = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
